# Сессия 2026-06-24/25 — Настройка Hermes DevOps + ai-engineer

## Итоги сессии

### Создано / настроено

| Компонент | Где | Статус |
|---|---|---|
| **DevOps Hermes** (default профиль) | CT 106 (192.168.1.15) | ✅ Работает |
| **ai-engineer** (второй профиль) | CT 106 (192.168.1.15) | ✅ Работает |
| **Telegram-бот** (pve-tgbot) | PVE хост (192.168.1.202) | ✅ Обновлён, интеграция с Hermes |
| **6 DevOps скиллов** | `/root/.hermes/skills/pve-*` | ✅ Активны |
| **3 ai-engineer скилла** | `/root/.hermes/skills/hermes-*` | ✅ Активны |
| **full-time-memory.md** | `/root/.hermes/full-time-memory.md` | ✅ Вечный журнал |
| **Буфер диалога** | `/var/tmp/hermes-history/<chat_id>` | ✅ На хосте PVE |

### Репозитории GitHub

| Репо | Содержимое |
|---|---|
| `AlexSavicky/hermes-config` | SOUL.md, AGENTS.md, MEMORY.md, full-time-memory.md, 9 скиллов |
| `AlexSavicky/bash` | `unified-pve-tgbot-v4.sh` — бот с Hermes-интеграцией |

### Исправленные баги

1. ❌ `192.168.1.1` (роутер) → ✅ `192.168.1.202` (Proxmox) во всех файлах
2. ❌ `web` в disabled_toolsets → ✅ убран, web_search работает
3. ❌ `skills` не в toolsets → ✅ добавлен в глобальные + platform_toolsets.cli
4. ❌ `hermes -z` (stateless) → ✅ буфер диалога через `/var/tmp/hermes-history/`
5. ❌ ai-engineer без инструментов → ✅ terminal, file, web, skills

## Как запустить в новой сессии

### DevOps Hermes (default)
```bash
# В CT 106:
hermes -z "проверь контейнеры"
hermes         # интерактивный TUI

# Через Telegram:
# Написать боту любой текст без /
```

### ai-engineer
```bash
# В CT 106:
export PATH="/root/.local/bin:$PATH"
ai-engineer -z "проверь SOUL.md DevOps Hermes на ошибки"
ai-engineer chat  # интерактивный TUI
```

### Обновить файлы из GitHub
```bash
# На хосте PVE (бот):
wget https://raw.githubusercontent.com/AlexSavicky/bash/main/unified-pve-tgbot-v4.sh -O /root/pve-tgbot.sh
systemctl restart pve-tgbot

# В CT 106 (конфиги Hermes):
cd /tmp && git clone https://github.com/AlexSavicky/hermes-config.git
cp /tmp/hermes-config/SOUL.md /root/.hermes/
cp /tmp/hermes-config/AGENTS.md /root/.hermes/
cp -r /tmp/hermes-config/skills/pve-* /root/.hermes/skills/
cp -r /tmp/hermes-config/skills/media-stack /root/.hermes/skills/
cp -r /tmp/hermes-config/skills/ai-engineer/hermes-* /root/.hermes/skills/
cp -r /tmp/hermes-config/skills/ai-engineer/hermes-* /root/.hermes/profiles/ai-engineer/skills/
rm -rf /tmp/hermes-config
```

### Пароли и доступы
- PVE хост: `192.168.1.202`, root / `)J27tjv5phaq`
- CT 106 (hermes): `192.168.1.15`, root / `)J27tjv5phaq`
- SSH с Windows не работает (нет sshpass). Использовать paramiko через Python или pct enter.

## Архитектура памяти

```
/root/.hermes/full-time-memory.md     ← Вечный журнал (append-only, 224 строки)
/root/.hermes/memories/MEMORY.md      ← Рабочая память (~800 токенов)
/var/tmp/hermes-history/<chat_id>     ← Буфер диалога Telegram
```

## Модели

| Инстанс | Модель |
|---|---|
| hermes (DevOps) | `glm-5.2` |
| ai-engineer | `ollama-cloud/deepseek-v4-pro` |

## Известные проблемы / TODO

- [ ] Hermes TUI всё ещё показывает "0 skills" пока не перезапущен — проверить после выхода/входа
- [ ] SyncPlay в Jellyfin — не исследовано до конца
- [ ] unattended-upgrades — не установлены (предложить владельцу)
- [ ] Векторная БД для памяти — отложено, вернуться позже
- [ ] Rutracker интеграция — креды в .env, не протестировано
